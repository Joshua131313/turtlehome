import React from 'react';
import PropTypes from 'prop-types';
import './Title.css'


const Title = props => {
    return (
        <div className='apptitle'>
            
        </div>
    );
};

Title.propTypes = {
    
};

export default Title;