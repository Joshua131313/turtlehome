import React from 'react';
import PropTypes from 'prop-types';
import './Reels.css'

const Reels = props => {
    return (
        <div>
            
        </div>
    );
};

Reels.propTypes = {
    
};

export default Reels;