import React from 'react';
import PropTypes from 'prop-types';
import './Media.css'

const Media = props => {
    return (
        <div>
            
        </div>
    );
};

Media.propTypes = {
    
};

export default Media;