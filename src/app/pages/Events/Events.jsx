import React from 'react';
import PropTypes from 'prop-types';
import './Events.css'

const Events = props => {
    return (
        <div>
            
        </div>
    );
};

Events.propTypes = {
    
};

export default Events;